# 客户端视觉对齐验证 — 1.1.1

以用户指定的 http://127.0.0.1:5173/ 为参考，独立浏览器读取画面，使用隔离目录启动真实打包客户端验证。

- 网页入口、数据、场景、循环算法、主样式、响应式样式和开场文件 SHA-256 与修改前相同。桌面专用入口复用原三维场景、模型、灯光、镜头、动画和字体样式。
- 相同 1440×900 CSS 视口下，品牌、导航、档案说明、列导航、页脚及三维画布位置/尺寸误差均小于 0.5 CSS px（Chromium 与 Windows DPI 舍入）。两端截图已检查。
- 首屏不显示旧绿色编辑布局或简化小阵列。ARCHIVE INDEX / SAVED 打开完整本地知识库，详情 EDIT 编辑本地 Markdown。
- 原始40份示例内容和槽位一致；编辑内容以磁盘 Markdown 为准。40个展陈槽位不限制完整文档数。超过40篇选择可在刷新后保留，空槽显示新建提示。
- 真实客户端通过新建、保存、Markdown 表格/禁用 HTML、全文检索、编辑按键隔离、未保存取消/放弃、回收区删除/恢复、暗色设置、重启持久化、1000×680 窗口、空库和单篇文档检查。
- pageerror=[]，资源 HTTP 错误=[]，运行请求不包含 http/https，Node 未暴露。原网页构建和PWA、类型检查、仓库持久化/冲突/权限测试通过。
- 源码与截图见本目录 desktop-alignment；详细机器结果 result.json。开场 Novecento 沿用项目既有回退字形，未新增字体分发授权。未重新开展全部 Windows 硬件/睡眠恢复验证。
